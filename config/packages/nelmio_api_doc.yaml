nelmio_api_doc:
    documentation:
        info:
            title: KSP REST API
            description: REST API
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc

when@prod:
    nelmio_api_doc:
        enabled: false
