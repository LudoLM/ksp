/*
import { useUserStore } from "../store/user";
import { useRouter } from "vue-router";

export async function useLogout() {
  const userStore = useUserStore();
  const router = useRouter();

  // Déconnecte l'utilisateur
  userStore.logout();

  // Redirige vers la page d'accueil avec un message d'alerte
  window.lo

  // Lance une erreur pour signaler une déconnexion
  throw new Error('Erreur 401 : Token expiré ou utilisateur non autorisé.');
}
*/
